"""Shared mock engine for the AI-EDU Python projects.

Mirrors the JavaScript engine in /src/lib/js so the two ecosystems produce the
same results. Pure standard library — no dependencies, fully offline.
"""
